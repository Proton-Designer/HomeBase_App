import React from 'react';
import { NotificationSettings } from '../../components/settings/NotificationSettings';

export default function HomeownerNotificationSettingsScreen() {
  return <NotificationSettings showPayouts={false} />;
}
